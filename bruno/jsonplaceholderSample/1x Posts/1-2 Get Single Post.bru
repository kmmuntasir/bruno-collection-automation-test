meta {
  name: 1-2 Get Single Post
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/posts/1
  body: none
  auth: none
}

script:post-response {
  // console.log("Sample Post-Response Script");
}

tests {
  test("ID matches the UserId", () => {
    expect(res.body.id).to.equal(1);
  });
  
}
