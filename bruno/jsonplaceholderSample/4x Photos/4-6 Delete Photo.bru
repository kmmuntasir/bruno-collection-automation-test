meta {
  name: 4-6 Delete Photo
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/photos/1
  body: none
  auth: none
}

assert {
  res.status: eq 200
}

tests {
  test("Status should be 200", () => {
    expect(res.status).to.equal(200);
  });
  
  test("Response should be empty object", () => {
    expect(res.body).to.be.an("object");
    expect(Object.keys(res.body)).to.have.length(0);
  });
}
